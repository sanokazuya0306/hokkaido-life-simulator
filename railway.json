{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "reflex_app/nixpacks.toml",
    "watchPatterns": ["reflex_app/**"]
  },
  "deploy": {
    "startCommand": "cd reflex_app && caddy start --config Caddyfile && reflex run --env prod --backend-only --loglevel warning",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
